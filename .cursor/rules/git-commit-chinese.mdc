---
description: Git 提交信息使用简体中文，正文分点列出变更
alwaysApply: true
---

# Git 提交信息（简体中文 + 分点）

生成或编写 **任何** Git commit message 时（含 Agent 执行 `git commit`、用户要求生成提交说明），必须遵守：

## 语言

- 全文使用**简体中文**（Conventional Commits 的 type 前缀可保留英文：`feat` / `fix` / `refactor` / `docs` / `chore` / `test` / `perf` / `build` / `ci`）
- 除代码标识符、寄存器名、协议常量外，不使用英文句子

## 格式

**单行变更**（仅一处小改动）：

```
fix: 修复软 I2C 总线忙时未释放 SCL
```

**多处变更**（默认采用此格式）：

```
feat: 支持 BMI088 加速度计初始化

- 新增 SPI 模式配置与芯片 ID 校验
- 增加初始化失败重试逻辑
- 更新 imu 模块错误日志输出
```

规则：
- 第一行：`<type>: <简短摘要>`，摘要 ≤ 72 字，概括主要目的
- 空一行后，用 `- ` 开头的列表逐条说明变更（2–5 条为宜）
- 每条写**做了什么、为何**，不要堆砌文件名
- 按逻辑分组，不要一条 diff 一行

## 示例

```
fix: 修复 EEPROM 写入超时

- 延长页写等待时间至 10ms
- 写入失败时增加总线复位
- 补充超时错误码返回
```

```
refactor: 重构 I2C 驱动为统一接口

- 抽取软/硬 I2C 公共读写流程
- 统一错误码与日志格式
- 移除重复的延时实现
```

## 禁止

- 不要用英文写 subject 或 body
- 不要写 `Update files`、`Fix bug` 等空泛说明
- 不要把完整 diff 复制进提交说明
