# Git 提交信息生成规则（Source Control ✨ 生成提交说明）

当 Cursor 根据 staged 变更自动生成 Git commit message 时，必须严格遵守：

## 语言
- 全文使用简体中文
- type 前缀可用英文：feat / fix / refactor / docs / chore / test / perf / build / ci

## 格式

单行小改动：
```
fix: 修复软 I2C 总线忙时未释放 SCL
```

多处变更（优先使用）：
```
feat: 支持 0x5A 广播写机器附加信息

- 新增广播写协议解析
- 校验附加信息长度与 CRC
- 写入 EEPROM 对应存储区
```

要求：
1. 第一行：`<type>: <简短中文摘要>`，≤ 72 字
2. 空一行
3. 正文用 `- ` 列表分点说明每项变更（2–5 条）
4. 每条说明「做了什么」，不要只列文件名
5. 禁止英文句子（代码符号除外）

## 示例

```
fix: 修复 BMI088 初始化失败

- 修正 SPI 模式配置顺序
- 增加芯片 ID 校验重试
- 更新错误日志输出
```
